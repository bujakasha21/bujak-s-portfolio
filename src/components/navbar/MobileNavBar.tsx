import React, { useState } from "react";

const MobileNavBar = () => {
  const [isActive, setIsActive] = useState(false);

  return <div className="flex md:hidden">MobileNavBar</div>;
};

export default MobileNavBar;
