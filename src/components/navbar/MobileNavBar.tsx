import React, { useState } from "react";

const MobileNavBar = () => {
  const [isActive, setIsActive] = useState(false);

  return <div className="flex md:hidden">la</div>;
};

export default MobileNavBar;
